---
description: Deploy the static site with pnpm ship
alwaysApply: true
---

# Deploy

Site publish is `pnpm ship` (Wrangler Pages project `agent-facts`, uploads `site/` with no build step).
